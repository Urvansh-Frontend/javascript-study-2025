let index = 1
while (index <= 10) {
    // console.log(` Value of index is ${index}`)
    index = index + 2;
}

let myarray = ["Urvansh", "Kunal", "Kishna", "Batman"]
let arr = 0;
while (arr < myarray.length) {
    console.log(`${myarray[arr]}`)
    arr++;
}

// Checks condition at end 
let score = 16;
do {
    // console.log(`My score is ${score}`)
    score++;

} while (score <= 10);